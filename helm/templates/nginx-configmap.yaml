{{- if and .Values.enabled .Values.nginxConfig.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "%s-nginx-config" (include "but-better-geojson-viewer.fullname" .) }}
data:
  default.conf: |
{{- .Values.nginxConfig.conf | nindent 4 }}
{{- end }}
